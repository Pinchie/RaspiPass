-Timezone/locale adjustment - this can't be done using wifi locale due to countries spanning multiple timezones. Can it be done as a headless command?
-Reset to defaults option on configuration tab - use US as locale
-Track previously-used MAC addresses to prevent chance of repeating the same AP within Nintendo's time limit 
-Whitelist Raspbian repositories for apt-get 
-apt-get upgrade method from web frontend 
-Installation script to set up users & directories, to allow downloading of git repo and installation without having to download RaspiPass image
-The above installation script will also need to install necessary packages (eg macchanger, hostapd, randomize-lines)
